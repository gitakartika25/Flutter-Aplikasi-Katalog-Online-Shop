import 'package:flutter/material.dart';

const kTextColor = Color(
    0xFF535353); // mendefinisikan nilai TextColor yang akan dipakai pada setiap objek text
const kTextLightColor = Color(
    0xFFACACAC); // mendefinisikan nilai TextLightColor yang akan dipakai pada setiap objek text

const kDefaultPaddin =
    20.0; // mendefinisikan nilai DefaultPaddin yang akan dipakai untuk mengatur padding
